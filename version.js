define(function () { return JSON.parse(
'{"version_number":"2.3.5.20","version_description":"added modal for active verification sessin with contact, added chat bottom window for incoming session request, changed chat head encrypted devices buttons, added active session button in chat head, added active session chat item icon, added chat system messages if verification session with contact has failed, changed fingerprint string generation, changed how trusted-items are signed and verified, fixed error with parsing own trusted devices, fixed error with autorejected incoming verification sessions,"}'
)});