<template>
    <div class="chat-body-content-placeholder-text no-select vue-migrated">{{ xb.getString("placeholder_loading") }}</div>
</template>

<script setup>
import { useXabber } from '../../composables/useXabber.js';

const xb = useXabber();

let backboneView = null;

function setBackboneView(view) {
    backboneView = view;
}

defineExpose({
    setBackboneView,
});
</script>
